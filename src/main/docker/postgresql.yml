version: '2'
services:
  ecart-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/Ecart/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=Ecart
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
